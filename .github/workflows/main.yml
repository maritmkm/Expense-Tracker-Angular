# name: Build, Test, and Deploy Angular (GitHub Pages + Private Docker Hub)

# on:
#   push:
#     branches:
#       - main

# permissions:
#   contents: write
#   packages: write

# jobs:
#   build-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 18

#       - name: Install dependencies
#         run: npm ci

#       - name: Security audit
#         run: npm audit --audit-level=high || true

#       # ⭐ Build Angular with correct base-href
#       - name: Build Angular app
#         run: npm run build -- --configuration production --base-href "/my-app/"

#       # ⭐ Deploy to GitHub Pages using your real output folder (dist/my-app)
#       - name: Deploy to GitHub Pages
#         uses: JamesIves/github-pages-deploy-action@v4
#         with:
#           branch: gh-pages
#           folder: dist/my-app/browser

#       # ⭐ Login to Docker Hub
#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       # ⭐ Build Docker image using your project name (my-app)
#       - name: Build Docker image
#         run: |
#           IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/my-app
#           docker build -t $IMAGE_NAME:latest -t $IMAGE_NAME:${{ github.sha }} .

#       # ⭐ Run health check before pushing
#       - name: Run Docker container for testing
#         run: |
#           IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/my-app
#           docker run -d -p 8080:80 --name angular-test $IMAGE_NAME:latest
#           sleep 5
#           curl -I http://localhost:8080 || (docker logs angular-test && exit 1)
#           docker stop angular-test
#           docker rm angular-test
#           echo "Container OK!"

#       # ⭐ Push Docker image
#       - name: Push Docker image
#         run: |
#           IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/my-app
#           docker push $IMAGE_NAME:latest
#           docker push $IMAGE_NAME:${{ github.sha }}

name: Build, Test, and Deploy Angular (GitHub Pages + Private Docker Hub)

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build Angular app
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist/my-app
